interface:
  display_name: Orchestrate
  short_description: Coordene subagentes em tarefas complexas
  default_prompt: Use $orchestrate para dividir esta tarefa em frentes independentes
    e consolidar os resultados.
policy:
  allow_implicit_invocation: true
